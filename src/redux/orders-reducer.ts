let initialState = {
  orders: [],
};

const ordersReducer = (state = initialState, action: any) => {
  switch (action.type) {
    default:
      return state;
  }
};



export default ordersReducer;
